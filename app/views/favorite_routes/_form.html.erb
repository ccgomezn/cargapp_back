<%= form_with(model: favorite_route, local: true) do |form| %>
  <% if favorite_route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite_route.errors.count, "error") %> prohibited this favorite_route from being saved:</h2>

      <ul>
        <% favorite_route.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :origin_city_id %>
    <%= form.text_field :origin_city_id %>
  </div>

  <div class="field">
    <%= form.label :destination_city_id %>
    <%= form.text_field :destination_city_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
