<%= form_with(model: company_user, local: true) do |form| %>
  <% if company_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_user.errors.count, "error") %> prohibited this company_user from being saved:</h2>

      <ul>
        <% company_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
